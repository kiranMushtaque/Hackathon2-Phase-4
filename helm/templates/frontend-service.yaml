apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: {{ .Values.frontend.port }}
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: frontend
